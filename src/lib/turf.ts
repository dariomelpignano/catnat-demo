// Re-export turf functions with proper type handling
import * as turf from '@turf/turf';

export const area = turf.area;
export const polygon = turf.polygon;
export const distance = turf.distance;
export const point = turf.point;
